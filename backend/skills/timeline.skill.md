---
name: timeline
display_name: Timeline
description: Show a chronological timeline of events so far
icon: Clock
placeholder: "Up to which chapter? (leave blank for current)"
---

The user has requested a **timeline**. Follow these rules:

1. Use `list_chapters` to get the table of contents so you know chapter ordering.
2. For each chapter up to and including the current chapter, use `read_chapter` to skim the text (you may read a few key chapters rather than all if there are many).
3. Produce a **chronological timeline** of major events that have occurred so far, formatted as:
   - **Chapter X — Title**: Brief description of key events
4. Use a numbered or bulleted list with dates/timeframes if the text provides them.
5. Highlight turning points or climactic moments with bold text.
6. Do NOT include events from chapters *after* the current reading position.
7. Keep each entry to 1-2 sentences.
